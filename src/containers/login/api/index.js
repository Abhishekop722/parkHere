import { apiUrl } from "../../../settings";


export const nativeLoginUrl = () => {
    return apiUrl + '/api/login/';
};
export const RegisterUrl = () => {
    return apiUrl + '/api/parking-lot/';
};